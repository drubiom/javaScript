#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define CANTIDAD

int main(int argc, char *argv[]){
    const char *elegido;
    int aleatoria;
    char adivinado[29]
    srand(time(NULL));
    aleatoria = rand () % CANTIDAD (palabra);
    elegida = palabra[aleatoria];
    
    return EXIT_SUCCESS;
}
